# Browser (Client Component) calls — relative path proxied by next.config.mjs.
NEXT_PUBLIC_API_BASE_URL=/api

# Server (Server Component) calls — absolute backend URL, no proxy on the server.
API_INTERNAL_URL=http://localhost:5080/api
